version: '2.7'

services:
  db:
    image: postgres
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8888
    volumes:
      - .:/InstagramClone
    ports:
      - "8888:8888"
    depends_on:
      - db